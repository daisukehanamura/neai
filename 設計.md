# neai — iPhone XR 常設AI端末 / アーキテクチャ決定

`要件.txt`（AIとの壁打ちメモ）を土台に、公式ドキュメントで事実確認したうえで方向を確定したもの。
両者が食い違う場合は**この文書が優先**する。差分は末尾「メモから修正した前提」を参照。

---

## 1. 確定した4つの決定

| # | 論点 | 決定 | 根拠 |
|---|------|------|------|
| 1 | バックエンドの厚み | **薄い / WebRTC直結** | 音声はブラウザ↔OpenAI直結。中継ホップ0でレイテンシ目標1秒が現実的。ブラウザのAECがそのまま使える |
| 2 | FE/ホスティング | **Vite + React SPA を Workers に同梱** | 単一画面のキオスクにSSR不要。1ドメイン・1デプロイでCORSも認証も一系統 |
| 3 | カメラ起動 | **モデルの Function Calling** | クライアント側の文言判定を持たない。想定外の言い回しにも追従する |
| 4 | 認証 | **デバイストークン + クォータ** | 無人常設端末なので再ログインが発生しない方式。コスト暴走はWorker側の上限で止める |

---

## 2. 全体構成

```
                        ┌─────────────────────────────┐
                        │  Cloudflare Worker          │
   ┌── HTTPS ──────────>│  ・静的アセット配信 (SPA)     │
   │                    │  ・POST /api/session        │──> OpenAI
   │                    │  ・POST /api/tools/*        │    /v1/realtime/calls
iPhone XR               │  ・KV: 1日クォータ            │
 (Safari 前面表示)       │  Secrets: OPENAI_API_KEY 他  │
   │                    └─────────────────────────────┘
   │
   └── WebRTC (音声 + データチャネル "oai-events") ────────> OpenAI Realtime
        ※ Cloudflare はこの経路に居ない
```

**重要**：メディアはブラウザとOpenAIの間を直接流れる。Worker はセッション確立の一往復と、
ツール実行時のHTTPリクエストにしか登場しない。

### セッション確立方式

OpenAI は2方式を提供している。本プロジェクトは **Unified Interface** を採用する。

- **採用：Unified Interface** — ブラウザが SDP offer を Worker に送り、Worker が session config
  （instructions / tools / voice / model）を添えて `POST /v1/realtime/calls` を叩き、answer SDP を返す。
  クライアントにトークンすら渡らず、プロンプトとツール定義もサーバ側に留まる。
- 代替：Ephemeral Token — Worker が `POST /v1/realtime/client_secrets` で短命キーを発行し、
  ブラウザが自分で `/v1/realtime/calls` に繋ぐ。Unified がうまく動かない場合のフォールバック。

いずれもセットアップ時の1往復のみで、通話中の遅延には影響しない。

### モデル

実装時点のドキュメント例は `gpt-realtime-2.1`。**着手時に公式ドキュメントで再確認する**。
コスト次第で mini 系への切り替えを検討（`worker/config.ts` の一箇所で差し替えられるようにする）。

---

## 3. リポジトリ構成

```
neai/
├ src/                   Vite + React + TypeScript (SPA)
│  ├ App.tsx             状態表示のみの単一画面
│  ├ realtime/
│  │  ├ connection.ts    RTCPeerConnection / データチャネル / 自動再接続
│  │  ├ media.ts         getUserMedia({audio,video}) を1回で取得し保持
│  │  └ tools.ts         look_at_camera 等のクライアント側ハンドラ
│  └ auth.ts             デバイストークンの保存と付与
├ worker/
│  ├ index.ts            ルーティング + 認証ミドルウェア
│  ├ session.ts          POST /api/session (SDP中継 + session config)
│  ├ tools/              weather / shopping-list / clock ... (Phase 4以降)
│  └ config.ts           モデル名・instructions・tools定義
├ wrangler.toml
├ 要件.txt               元メモ（履歴として残す）
└ 設計.md                この文書
```

デプロイは `wrangler deploy` の1コマンド。

---

## 4. シーケンス

### 通常の音声会話（目標：発話終了から1秒前後で音声開始）

```
[起動時1回] tap → getUserMedia({audio:true, video:true}) → Wake Lock 取得
             ↓
ユーザー発話 → WebRTC で OpenAI へ直送
             ↓  (server VAD が発話終了を検出)
OpenAI → 音声ストリームを WebRTC で返す → 即再生
```

### 画像付き質問（目標：1〜3秒で音声開始）

```
「これ何？」
   ↓  WebRTC
OpenAI → データチャネルに function_call: look_at_camera
   ↓
ブラウザ: 既に流れている video トラックから canvas.drawImage で1フレーム抜き出し
          → JPEG base64 (長辺 1024px 程度に縮小)
   ↓
conversation.item.create { type: "input_image", image_url: "data:image/jpeg;base64,..." }
+ function_call_output
   ↓
response.create → 音声回答
```

video トラックは **WebRTC に載せない**（ローカル保持のみ）。帯域とコストを使わず、
フレーム取得は数ミリ秒で済む。

---

## 5. 認証とコスト制御

```
初回のみ:  https://<host>/#k=<64文字ランダム>  → localStorage に保存、URLからは即除去
毎リクエスト: Authorization: Bearer <key>
Worker:    定数時間比較で検証 → KV で 1日あたりのセッション数/合計秒数に上限
```

- OpenAI APIキーは Worker Secrets のみに存在。クライアントJSにもGitにも入れない。
- アイドル60秒で WebRTC を自動切断する（実装済み / `IDLE_LIMIT_SEC`）。
  課金はトークン単位だが、接続中はマイク音声を送り続けるため放置ぶんが費用になる。
  つなぎっぱなしは gpt-realtime-2.1 で1時間あたり約170円、24時間×30日で約12万円。
- 画面に実費用を表示する（実装済み）。`response.done` の実測トークン数から計算する。
  価格表は `src/pricing.ts`。**モデルを変えたら価格表も更新すること。**
- ウェイクワード待機中は音声をOpenAIへ送らない（Phase 3、オンデバイス検出）。

---

## 6. iOS実機で最初に潰す検証項目

> **2026-08-29 検証済み（iPhone XR / iOS 17.5.1 / Safari 17.5）**
>
> - **一括取得：合格。** 音声＋映像（720x1280 / 30fps / 背面）が同時に `live` を維持する。
> - **フレーム取得：合格。ただし 576x1024 / 178KB で 328ms かかる。**
>   JPEGエンコードがメインスレッドで走るため。画像質問の1〜3秒予算に必ず織り込むこと。
>   短縮の余地：長辺を512pxへ / 品質を下げる / OffscreenCanvas をワーカーで回す。
> - **2本目の `getUserMedia({video})` は1本目の映像トラックを `ended` にする。**
>   iOSは同時に1つのカメラしか掴めない。音声トラックは生き残る。
>   → 実装上の禁止事項：**通話中に `getUserMedia` を再呼び出ししない。**
>     起動時に一括取得し、以後は保持し続ける。
> - **Wake Lock：想定通り。** 画面が hidden になると解放され、visible で再取得できる。
>   `visibilitychange` での再取得処理は必須。
> - **エコーキャンセル：良好の見込み。** 詳細は下記「解決済み：音の出力先」。

Phase 1のコードを書く前に、この3つを iPhone XR 実機で確認する。ここで詰むと構成が変わる。

1. **マイクとカメラの同時保持**
   WebRTC通話中に別途 `getUserMedia({video})` を呼ぶとマイクが切れる可能性がある。
   → 起動時に `{audio:true, video:true}` を**1回で**取得し、両トラックを保持し続ける方式で回避できるか。
2. **エコーキャンセル**
   スピーカーとマイクが同一筐体。AIが自分の声を拾って割り込まないか。
   → `echoCancellation: true` で足りるか、音量とマイク距離の調整が要るか。
3. **長時間安定性**
   Wake Lock を保持したまま数時間、WebRTC再接続が正しく回るか。

補足：
- iPhone XR の iOS 上限は 18（iOS 26 で対象外）。Wake Lock API は 16.4+ なので問題なし。
- 画面ON + 常時給電はバッテリー膨張リスクがある。UIは黒基調・輝度最小にする。


### 解決済み：音の出力先

**2026-08-29 に一度リスクとして立てたが、実測で解消した。**

当初、マイク取得中に音が受話口（画面上部）から小さく鳴る現象を観測し、
「常設端末として成立しない」最大のリスクとして扱った。
W3C Audio Session の説明書も `play-and-record` を
「スピーカーではなく受話口へ回すためのもの」と記述している。

`spike/routing.html` で総当たりした結果、**この現象は再現しなかった。**

| 条件 | 聞こえ方 | マイクの拾い |
|---|---|---|
| マイク取得中 + WebAudio（音声＋映像で取得） | スピーカー | -31.5 dBFS |
| マイク取得中 + WebAudio（音声のみで取得） | スピーカー | -31.0 dBFS |
| `audioSession.type = "playback"` | スピーカー | -80.9 dBFS |
| `audioSession.type = "play-and-record"` | スピーカー | -27.2 dBFS |
| **WebRTCループバック（音声のみ）** | スピーカー | -34.7 dBFS |
| **WebRTCループバック（映像あり）** | スピーカー | **-73.1 dBFS** |

- **どの条件でもスピーカーから鳴る。** 「映像トラックを保持していないと受話口になる」
  という仮説を立てて検証したが、音声のみの取得でもスピーカーだった。**反証済み。**
  実装上の制約にはならない。
- **マイクはどの条件でも生存する。** `type = "playback"` 中に話しかけても
  -30.0 dBFS で拾えた（通常時は -27.7 dBFS）。設定でマイクが死ぬ組み合わせは見つからなかった。
- **エコーキャンセルは効いている見込み。** 第1段階の実経路にあたる
  WebRTCループバック（映像あり）で、スピーカーから聞こえているのに拾いは -73.1 dBFS。
  WebAudio直接再生の -31.5 dBFS と比べて40dB以上低い。
  ただし音量条件を揃えた計測ではないため、**確定は第1段階で実際のAI音声を使って行う。**

**未解明として残る点：** 最初に観測した受話口の現象が何によって起きたのかは特定できていない。
再現した条件との差は `noiseSuppression: false` / `autoGainControl: false` を
指定していた点。本アプリはこれらを既定値（有効）で使うため実害は低いと判断し、追跡を打ち切る。
第1段階で受話口の症状が出た場合は、まずこの2つの制約を疑うこと。

---

## 7. 実装フェーズ

| Phase | 内容 | 完了条件 |
|---|---|---|
| 0 | 上記「iOS実機検証」3項目のスパイク | 3項目とも実機で確認済み |
| 1 | タップ開始の音声会話（Worker + SPA + WebRTC） | 発話→回答開始のレイテンシを実測し1秒前後 |
| 2 | `look_at_camera` ツールで画像質問 | 「これ何？」が1〜3秒で回答開始 |
| 3 | ウェイクワード「ねえAI」 | オンデバイス検出、待機中は無通信 |
| 4 | Function Calling（天気 / 時刻 / 買い物リスト） | Worker側ツール3種 |
| 5 | デバイストークン認証 + クォータ + 本番デプロイ | 自分の端末以外から使えない |
| 6 | 会話履歴・長期記憶（D1 or DO SQLite） | セッションを跨いだ文脈保持 |
| 7 | カメラ常時監視・イベント検出 | — |

Phase 5 の認証は本来もっと早く入れたいが、Phase 1〜2 はローカルネットワークでの
開発なので後回しでよい。**インターネットに公開する前に必ず Phase 5 を通す**。

### ウェイクワード（Phase 3・方式を再検討中）

**端末に触らず話しかけて操作する**のがこのプロジェクトの核心要件。コスト対策ではない
（コストは Phase 1 で入れた60秒の自動切断で既に解決している）。

**2026-08-29：Picovoice Porcupine は採用できないことが判明。**
無料枠が 2026年6月30日で廃止され、既存の無料 AccessKey も無効化された。
有料プランは月額 $899 から。サインアップも法人メール必須に変わっている。

代替候補：

| 方式 | 可否 | 備考 |
|---|---|---|
| **Vosk（Kaldi WASM）** | **iOS Safari で動作確認済み** | Apache-2.0 / アカウント不要 / 日本語モデルあり（47MB）。採用の見込み |
| openWakeWord + onnxruntime-web | 次点 | 学習済みモデルは英語のみ。日本語は自前学習が要る |
| Whisper tiny + ローカルVAD | 保留 | A12 での速度が不明。常時稼働は厳しい可能性 |
| iOS の Web Speech API | 不採用 | 連続待機に使えない（ジェスチャ必須・無音で停止） |

#### Vosk の検証結果（2026-08-30 / iPhone XR / iOS 17.5.1）

- **モデルの読み込みと認識、どちらも iOS Safari で動いた。** 最大の関門を通過。
- **遠くの声は届かない。** カフェ（最悪条件）では口元に近づけないと反応せず、
  入力ゲインを10倍にしてようやく実用の入口。静かな家での再調整が必要。
- **全文認識の精度は低い。** ただし「ねー」には反応した。
- **モデルは文法限定に対応している**（`graph/Gr.fst` と `HCLr.fst` が存在）。
  認識対象を数語句に絞れば精度が上がり CPU も下がる。これが本命の対策。

語彙表（20.7万語）を直接調べて確認した使用可能な語：

| 用途 | 使える語 | 使えない語 |
|---|---|---|
| ウェイクワード | `ねえ` `ねー` `ねぇ` `エーアイ` | `AI` `エイアイ` `えーあい` |
| ローカルコマンド | `タイマー` `一`〜`十` `十五` `二十` `三十` `分` `止めて` `停止` `やめて` `今` `何時` | `五分` `十五分`（分割して書く） |

`[unk]` を文法に必ず含めること。無いと無関係な音まで最も近い語句に強制的に当てはめる。

#### 残っている確認（静かな環境で行う）

1. 文法限定の ON / OFF で検出率がどう変わるか
2. 届く最小のゲイン（10倍のままだと雑音も増幅する）
3. 離れた位置での入力レベル（dBFS）。外付けマイクの要否はこれで判断する
4. CPU 使用率と発熱

### 検出器によらず確定している土台（実装済み）

方式が変わってもここは変わらない。Phase 0 の検証結果から導かれた制約。

- **付属のマイク管理は使えない。** 検出器が自分で `getUserMedia` を呼ぶ実装は、
  iOS でマイクを取り直せないため成立しない。
  → 自前の AudioWorklet で既存トラックから 16kHz / 16bit フレームを作る
    （`src/wakeword/downsampler.ts`）。
- **会話の切断でトラックを止めてはいけない。** 待機には生きたマイクが要る。
  → `src/media.ts` の `MediaController` が起動時に一度だけ取得し、保持し続ける。
    `RealtimeSession.stop()` は PeerConnection を閉じるだけでトラックに触らない。
- 検出器は動的読み込みにして初期表示を軽く保つ。
- 会話中は検出を止める。AI 自身の声での誤検出を避けるため。

未設定時はタップ開始へフォールバックするので、検出器が無くてもアプリは動作する。

---

## 8. 環境変数 / Secrets

| 名前 | 置き場所 | 用途 |
|---|---|---|
| `OPENAI_API_KEY` | Worker Secret | Realtime セッション確立 |
| `DEVICE_KEY_HASH` | Worker Secret | デバイストークンの検証 |
| `WEATHER_API_KEY` | Worker Secret | Phase 4 |
| `REALTIME_MODEL` | wrangler.toml vars | モデル名の切り替え |

`.dev.vars` をローカル用に置き、`.gitignore` に追加する。

---

## 9. メモから修正した前提

`要件.txt` は叩き台なので、以下は本文書で置き換えた。

1. **「iPhone → Cloudflare → OpenAI → WebRTC → iPhone」という中継構成（6章・7章）**
   → 誤り。WebRTCでは音声はブラウザとOpenAIを直結する。Cloudflare はメディア経路に入らない。
2. **Durable Objects をリアルタイム接続に使う（7章）**
   → MVPでは不要。リアルタイム接続はDOを通らない。DOが要るのはクォータ計数・長期記憶・
     複数端末の共有状態で、Phase 5以降の話。
3. **クライアント側で「画像が必要か」を判定する（12章）**
   → Function Calling でモデルに判断させる。判定ロジックを持たない。
4. **Next.js / Vercel（8章・18章）**
   → 単一画面のキオスクにSSRの利点がない。Vite SPA + Workers 単一デプロイにする。
5. **Cloudflare Access（9章）**
   → セッション期限による再ログインが無人常設端末と相性が悪い。デバイストークン方式にする。
