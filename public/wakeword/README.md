# ウェイクワードのモデルファイル置き場

**現在このディレクトリは使われていない。**

当初 Picovoice Porcupine を採用する前提で `src/wakeword/` を実装したが、
**Picovoice の無料枠は 2026年6月30日で廃止された**（既存の無料 AccessKey も無効化。
有料プランは月額 $899 から）。個人プロジェクトでは採用できない。

代替として Vosk を検証中。`spike/vosk.html` を参照。

`src/wakeword/` の実装自体は残してある。検出器の差し替え口として使えるため。
